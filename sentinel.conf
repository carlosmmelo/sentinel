# specify path to xsn.conf or leave blank
# default is the same as xsnCore
#xsn_conf=/home/evan82/.xsncore/xsn.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
